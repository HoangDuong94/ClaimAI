# Copy this file to `.env` and fill in your SAP AI Core service key.
# Do NOT commit the filled .env file.

# Required: Provide your AI Core service key as JSON.
# Tip: Create a service key for your AI Core instance in BTP and paste its JSON here.
# Typical fields: clientid, clientsecret, url, tokenurl, serviceurls (AI_API_URL)
AICORE_SERVICE_KEY={
  "clientid": "<your-client-id>",
  "clientsecret": "<your-client-secret>",
  "url": "https://<subdomain>.authentication.<region>.hana.ondemand.com",
  "tokenurl": "https://<subdomain>.authentication.<region>.hana.ondemand.com/oauth/token",
  "serviceurls": {
    "AI_API_URL": "https://api.ai.core.cfapps.<region>.hana.ondemand.com"
  }
}

# Optional: Limit model lookup by resource group or fix a specific deployment ID.
# If AICORE_DEPLOYMENT_ID is set, it takes precedence over AICORE_MODEL.
AICORE_MODEL=gpt-4.1
AICORE_RESOURCE_GROUP=
# AICORE_DEPLOYMENT_ID=

# Optional: If you use a Destination instead of AICORE_SERVICE_KEY, uncomment:
# DESTINATION_NAME=YourAiCoreDestination
# AICORE_DESTINATION=YourAiCoreDestination

